# `apps/agent`

Background agent service for workflows, matching, and sync jobs.

## Structure
- `src/mastra/index.ts` exports the Mastra runtime entrypoint
- `src/mastra/agents/shire-orchestrator.ts` defines the orchestration agent
