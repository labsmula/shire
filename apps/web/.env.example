# Privy — auth + embedded wallets (https://dashboard.privy.io)
# Create an app, copy its App ID here, then restart the dev server.
# When unset, the app falls back to the client-side demo wallet so it still runs.
NEXT_PUBLIC_PRIVY_APP_ID=
SHIRE_AGENT_CHAT_URL=http://localhost:3010/chat/role-aware-chat-agent
